---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: addons-rook-ceph-operator
  namespace: argocd
spec:
  syncPolicy:
    preserveResourcesOnDeletion: false
  generators:
    - merge:
        mergeKeys: [server]
        generators:
          - clusters:
              values:
                addonChart: rook-ceph
                addonChartVersion: 1.19.3
                addonChartRepository: https://charts.rook.io/release
              selector:
                matchExpressions:
                  - key: enable_rook_ceph
                    operator: In
                    values: ['true']
          - clusters:
              selector:
                matchLabels:
                  environment: dev
              values:
                addonChartVersion: 1.19.5 # promote this
          - clusters:
              selector:
                matchLabels:
                  environment: prod
              values:
                addonChartVersion: 1.19.3
  template:
    metadata:
      name: addon-{{name}}-{{values.addonChart}}
    spec:
      project: addons
      sources:
        - repoURL: '{{metadata.annotations.addons_repo_url}}'
          targetRevision: '{{metadata.annotations.addons_repo_revision}}'
          ref: values
        - chart: '{{values.addonChart}}'
          repoURL: '{{values.addonChartRepository}}'
          targetRevision: '{{values.addonChartVersion}}'
          helm:
            releaseName: '{{values.addonChart}}'
            ignoreMissingValueFiles: true
            valueFiles:
              - $values/configs/addons/environments/default/{{values.addonChart}}/values.yaml
              - $values/configs/addons/environments/{{metadata.labels.environment}}/{{values.addonChart}}/values.yaml
              - $values/configs/addons/regions/{{metadata.labels.region}}/{{values.addonChart}}/values.yaml
              - $values/configs/addons/cluster_type//{{metadata.labels.cluster_type}}/{{values.addonChart}}/values.yaml
              - $values/configs/addons/cluster_name/{{name}}/{{values.addonChart}}/values.yaml
      destination:
        namespace: '{{values.addonChart}}'
        name: '{{name}}'
      syncPolicy:
        automated: {}
        syncOptions:
          - CreateNamespace=true
