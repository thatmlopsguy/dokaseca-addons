---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: addons-kokumi
  namespace: argocd
spec:
  syncPolicy:
    preserveResourcesOnDeletion: false
  generators:
    - merge:
        mergeKeys: [server]
        generators:
          - clusters:
              values:
                addonKustomize: kokumi
                addonKustomizeNamespace: kokumi
                addonKustomizeVersion: v0.14.0
              selector:
                matchExpressions:
                  - key: enable_kokumi
                    operator: In
                    values: ['true']
          - clusters:
              selector:
                matchLabels:
                  environment: dev
              values:
                addonKustomize: kokumi
                addonKustomizeNamespace: kokumi
                addonKustomizeVersion: v0.14.0 # promote this
          - clusters:
              selector:
                matchLabels:
                  environment: prod
              values:
                addonKustomize: kokumi
                addonKustomizeNamespace: kokumi
                addonKustomizeVersion: v0.14.0
  template:
    metadata:
      name: addon-{{name}}-{{values.addonKustomize}}
    spec:
      project: addons
      source:
          repoURL: '{{metadata.annotations.addons_repo_url}}'
          path: 'kustomize/{{values.addonKustomize}}/{{values.addonKustomizeVersion}}'
          targetRevision: '{{metadata.annotations.addons_repo_revision}}'
      destination:
        namespace: '{{values.addonKustomizeNamespace}}'
        name: '{{name}}'
      syncPolicy:
        automated: {}
        syncOptions:
          - CreateNamespace=true
