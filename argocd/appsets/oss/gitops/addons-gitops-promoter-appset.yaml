---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: addons-gitops-promoter
  namespace: argocd
spec:
  syncPolicy:
    preserveResourcesOnDeletion: false
  generators:
    - merge:
        mergeKeys: [server]
        generators:
          - clusters:
              values:
                addonKustomize: gitops-promoter
                addonKustomizeNamespace: promoter-system
                addonKustomizeVersion: v0.5.0
              selector:
                matchExpressions:
                  - key: enable_gitops_promoter
                    operator: In
                    values: ['true']
          - clusters:
              selector:
                matchLabels:
                  environment: dev
              values:
                addonKustomize: gitops-promoter
                addonKustomizeNamespace: promoter-system
                addonKustomizeVersion: v0.18.2
          - clusters:
              selector:
                matchLabels:
                  environment: prod
              values:
                addonKustomize: gitops-promoter
                addonKustomizeNamespace: promoter-system
                addonKustomizeVersion: v0.3.0
  template:
    metadata:
      name: addon-{{name}}-{{values.addonKustomize}}
    spec:
      project: addons
      source:
          repoURL: '{{metadata.annotations.addons_repo_url}}'
          path: 'kustomize/{{values.addonKustomize}}/{{values.addonKustomizeVersion}}'
          targetRevision: '{{metadata.annotations.addons_repo_revision}}'
      destination:
        namespace: '{{values.addonKustomizeNamespace}}'
        name: '{{name}}'
      syncPolicy:
        automated: {}
        syncOptions:
          - CreateNamespace=true
